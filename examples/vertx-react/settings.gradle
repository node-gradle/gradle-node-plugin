rootProject.name = 'vertx-react'

include "webapp"

// This is necessary to get Gradle use the plugin from its source instead of fetching it from the repository
// Don't use that in your project
includeBuild "../../"
